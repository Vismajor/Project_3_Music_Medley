
<div id="wrapper">
<div id="show-block">
<div id="page-content-wrapper">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">

<br>



<% @collections.each do |collection| %>
<div class="collection-list">
<div class="collection-title"><%= link_to collection.title, collection %>:</div>
 <div class="collection-list-songs"><% collection.songs.each do |song| %>
 -<%= song.title %>
 <% end %></div>
 </div>
<% end %>

<%#= will_paginate @songs %>
            </div>
        </div>
    </div>
</div>
</div>
</div>
